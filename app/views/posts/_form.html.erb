<%= form_for Post.new do |f| %>  <br>
  <div class="form-group clearfix">
    <%= f.file_field :avatar, id:'uploadimage', class: 'form-control', style: 'width: 20%; float: right;', onchange: 'loadFile(event)' %>
  </div>
   <br>
  <img id="output"/>
  <script>
    var loadFile = function(event) {
      var reader = new FileReader();
      reader.onload = function(){
        var output = document.getElementById('output');
        output.src = reader.result;
      };
      reader.readAsDataURL(event.target.files[0]);
    };
  </script>
    <%= f.radio_button :is_private, 'private' %> Private
    <%= f.radio_button :is_public, 'public' %> Public
  <div class="form-group clearfix">
    <%= f.submit id:'postbutton', class: 'btn btn-primary btn-lg float-end' %>
  </div>
<% end %>